@model IEnumerable<_28938232_Assignment.Models.Event>

@{
    ViewBag.Title = "Index";
}

<img src="/Content/Image/icon-ft.jpg" align="left" /> </br> <h2><strong>FIT-TREE CLUBS</strong></h2>
</br></br>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Event1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.detail)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.location)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Contact)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Event1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.detail)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.location)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Contact)
            </td>
            <td>

                @Html.ActionLink("Details", "Details", new { id = item.id }) |

            </td>
        </tr>
    }

</table>
